const arr = [20, 10, 30, 40, 1, 2, 3, 45]
let i = 0;
const myNumber = 50;
if (i === '0' && i <=0){
while (i < arr.length) {
    let number = arr[i];
    console.log(number);
    i++;
}
}else if(!i){
    while (i < arr.length) {
        let number = arr[i];
        console.log(`this is ${number} number`);
        i++;
    }
}
else{
    console.log('do something for codition javascript bro');
}